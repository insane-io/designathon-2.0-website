

const MissionLogs = () => {
    return (
        <section className="min-h-screen w-full bg-background flex items-center justify-center border-t border-white/10 relative">
             <div className="absolute inset-0 bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-neutral-900 via-background to-background opacity-50" />
            <h2 className="text-4xl font-bold uppercase tracking-widest text-white/20 z-10">Mission Logs</h2>
        </section>
    );
};

export default MissionLogs;