

const Sponsors = () => {
    return (
        <section className="min-h-screen w-full bg-background flex items-center justify-center border-t border-white/10 relative">
             <div className="absolute inset-0 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-neutral-900 via-background to-background opacity-50" />
            <h2 className="text-4xl font-bold uppercase tracking-widest text-white/20 z-10">Sponsors</h2>
        </section>
    );
};

export default Sponsors;